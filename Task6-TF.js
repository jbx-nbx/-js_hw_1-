'use strict';
let message;
message = (login == 'Employee')?'Hello'
:(login == 'Director')?'Hello'
:(login == '') ? 'No login':'';


/*
IMPORTANT NOTE:  
i did pay attention to variable(login) which had not been declared in original program task.
yet i solved the task as requested.
*/ 